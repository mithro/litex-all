# Install requirements needed to use the lm32 soft CPU inside LiteX
-e git+https://github.com/mithro/litex-data-lm32.git#egg=litex-data-lm32
# Install the common requirements that LiteX needs
-r base.txt
